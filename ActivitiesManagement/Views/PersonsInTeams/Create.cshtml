@model ActivitiesManagement.Models.PersonsInTeams

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm())
{
    @Html.HiddenFor(m => m.Id)
    @Html.HiddenFor(m => m.TeamId)
    @*@Html.HiddenFor(m => m.ApplicationUserId)*@
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="form-group">
        @Html.LabelFor(model => model.ApplicationUserId, "ApplicationsUserId", htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            <label>Selectati Userul</label>
            @Html.DropDownListFor(m => m.ApplicationUserId, new SelectList(Model.UsersForTeams, "Value", "Text"),

             "Selectati Userul", new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.ApplicationUserId, "", new { @class = "text-danger" })

        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-default" />
        </div>
    </div>
}
<div>
    @Html.ActionLink("Back to List", "Index")
</div>
