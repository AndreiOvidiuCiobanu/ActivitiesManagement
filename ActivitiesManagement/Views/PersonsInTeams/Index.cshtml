@model IEnumerable<ActivitiesManagement.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in ViewBag.Users)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.Team.)
                </td>*@
            <td>
                @item.UserName
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Show", new { id = item.Id }) |
                @using (Html.BeginForm(actionName: "Delete", controllerName: "PersonsInTeams", routeValues: new { id = item.Id }))
                {

                    @Html.HttpMethodOverride(HttpVerbs.Delete)

                    <button class="btn btn-success" type="submit">Sterge userul</button>
                }
            </td>
        </tr>
    }

</table>
